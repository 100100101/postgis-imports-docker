FROM postgis/postgis:14-master
# # ENV CONTAINER_PG_INSTANCE_DIR_PATH=/var/lib/postgresql
# ENV INNER_POSTGRES_PORT=5432
# # ENV OUTER_POSTGRES_PORT=25432
# ENV POSTGRES_DB=gis
# ENV POSTGRES_USER=gisuser
# ENV POSTGRES_PASSWORD=pass
# # ENV POSTGRES_INITDB_ARGS=
# # ENV POSTGRES_INITDB_WALDIR=
# # ENV POSTGRES_HOST_AUTH_METHOD=trust
# ENV PGDATA=/var/lib/postgresql/data/pgdata
# ENV HOST_PGDATA=./pgdata
# # EXPOSE $OUTER_POSTGRES_PORT